def plusColumnReduce(A){
    #_;
    return (+%(A^T))^T;
}


def rangeFromVector(v){
    #logical;
    vlogic = v~1;
    #arithmetic;
    n = plusColumnReduce(v);
    u = Zero(1:n);
    j = 0;
    i = 0;
    print(toString(v));
    print(toString(|v|));
    print(toString(n));
    if (i > 0) {
      if (i > 0){ 
      j = j + 1;
      }
    }
    while (i < |v|[0]) {
      if (i > 0) {
          i = i + 1;
      }
      i = i + 20;
    }
}

A = rangeFromVector([3;0;1;2;0;5]);
print(toString(A));
